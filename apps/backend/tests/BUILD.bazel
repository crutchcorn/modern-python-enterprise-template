load("@rules_python//python:defs.bzl", "py_test")

py_test(
    name = "test_main",
    srcs = ["test_main.py"],
    deps = [
        "//apps/backend",
        "@pypi//fastapi",
        "@pypi//httpx",  # keep: required by fastapi.testclient at runtime
    ],
)
